<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ProfileActivity"
    android:background="@color/thinksync_background">

    <ScrollView
        android:id="@+id/profile_ScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation"
        android:fillViewport="true">

        <LinearLayout
            android:id="@+id/profile_LAYOUT_Content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:paddingTop="16dp">

            <LinearLayout
                android:id="@+id/profile_LAYOUT_TopRow"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/profile_TEXT_Email"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="🔐 Logged in as:"
                    android:textSize="18sp"
                    android:textColor="@color/thinksync_dark_text" />

                <Button
                    android:id="@+id/profile_BTN_Logout"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Logout"
                    android:backgroundTint="@color/thinksync_purple"
                    android:textColor="@color/white"/>
            </LinearLayout>

            <TextView
                android:id="@+id/profile_TEXT_UploadedHeader"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="📤 Uploaded Summaries"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/thinksync_dark_text" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/profile_RV_UploadedSummaries"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="150dp"
                android:nestedScrollingEnabled="false" />

            <TextView
                android:id="@+id/profile_TEXT_SavedHeader"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="💾 Saved Summaries"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/thinksync_dark_text" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/profile_RV_SavedSummaries"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="150dp"
                android:nestedScrollingEnabled="false" />
        </LinearLayout>
    </ScrollView>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:menu="@menu/bottom_nav_menu"
        android:background="@color/thinksync_background"
        app:itemIconTint="@color/thinksync_accent"
        app:itemTextColor="@color/thinksync_accent"/>
</RelativeLayout>
